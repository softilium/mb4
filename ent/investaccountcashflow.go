// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/rs/xid"
	"github.com/softilium/mb4/ent/investaccount"
	"github.com/softilium/mb4/ent/investaccountcashflow"
)

// InvestAccountCashflow is the model entity for the InvestAccountCashflow schema.
type InvestAccountCashflow struct {
	config `json:"-"`
	// ID of the ent.
	ID xid.ID `json:"id,omitempty"`
	// RecDate holds the value of the "RecDate" field.
	RecDate time.Time `json:"RecDate,omitempty"`
	// Qty holds the value of the "Qty" field.
	Qty float64 `json:"Qty,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the InvestAccountCashflowQuery when eager-loading is set.
	Edges                    InvestAccountCashflowEdges `json:"edges"`
	invest_account_cashflows *xid.ID
}

// InvestAccountCashflowEdges holds the relations/edges for other nodes in the graph.
type InvestAccountCashflowEdges struct {
	// Owner holds the value of the Owner edge.
	Owner *InvestAccount `json:"Owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e InvestAccountCashflowEdges) OwnerOrErr() (*InvestAccount, error) {
	if e.loadedTypes[0] {
		if e.Owner == nil {
			// The edge Owner was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: investaccount.Label}
		}
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "Owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InvestAccountCashflow) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case investaccountcashflow.FieldQty:
			values[i] = new(sql.NullFloat64)
		case investaccountcashflow.FieldRecDate:
			values[i] = new(sql.NullTime)
		case investaccountcashflow.FieldID:
			values[i] = new(xid.ID)
		case investaccountcashflow.ForeignKeys[0]: // invest_account_cashflows
			values[i] = &sql.NullScanner{S: new(xid.ID)}
		default:
			return nil, fmt.Errorf("unexpected column %q for type InvestAccountCashflow", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InvestAccountCashflow fields.
func (iac *InvestAccountCashflow) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case investaccountcashflow.FieldID:
			if value, ok := values[i].(*xid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				iac.ID = *value
			}
		case investaccountcashflow.FieldRecDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field RecDate", values[i])
			} else if value.Valid {
				iac.RecDate = value.Time
			}
		case investaccountcashflow.FieldQty:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field Qty", values[i])
			} else if value.Valid {
				iac.Qty = value.Float64
			}
		case investaccountcashflow.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field invest_account_cashflows", values[i])
			} else if value.Valid {
				iac.invest_account_cashflows = new(xid.ID)
				*iac.invest_account_cashflows = *value.S.(*xid.ID)
			}
		}
	}
	return nil
}

// QueryOwner queries the "Owner" edge of the InvestAccountCashflow entity.
func (iac *InvestAccountCashflow) QueryOwner() *InvestAccountQuery {
	return (&InvestAccountCashflowClient{config: iac.config}).QueryOwner(iac)
}

// Update returns a builder for updating this InvestAccountCashflow.
// Note that you need to call InvestAccountCashflow.Unwrap() before calling this method if this InvestAccountCashflow
// was returned from a transaction, and the transaction was committed or rolled back.
func (iac *InvestAccountCashflow) Update() *InvestAccountCashflowUpdateOne {
	return (&InvestAccountCashflowClient{config: iac.config}).UpdateOne(iac)
}

// Unwrap unwraps the InvestAccountCashflow entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (iac *InvestAccountCashflow) Unwrap() *InvestAccountCashflow {
	tx, ok := iac.config.driver.(*txDriver)
	if !ok {
		panic("ent: InvestAccountCashflow is not a transactional entity")
	}
	iac.config.driver = tx.drv
	return iac
}

// String implements the fmt.Stringer.
func (iac *InvestAccountCashflow) String() string {
	var builder strings.Builder
	builder.WriteString("InvestAccountCashflow(")
	builder.WriteString(fmt.Sprintf("id=%v", iac.ID))
	builder.WriteString(", RecDate=")
	builder.WriteString(iac.RecDate.Format(time.ANSIC))
	builder.WriteString(", Qty=")
	builder.WriteString(fmt.Sprintf("%v", iac.Qty))
	builder.WriteByte(')')
	return builder.String()
}

// InvestAccountCashflows is a parsable slice of InvestAccountCashflow.
type InvestAccountCashflows []*InvestAccountCashflow

func (iac InvestAccountCashflows) config(cfg config) {
	for _i := range iac {
		iac[_i].config = cfg
	}
}
