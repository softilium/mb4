// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/rs/xid"
	"github.com/softilium/mb4/ent/emission"
	"github.com/softilium/mb4/ent/ticker"
)

// EmissionCreate is the builder for creating a Emission entity.
type EmissionCreate struct {
	config
	mutation *EmissionMutation
	hooks    []Hook
}

// SetRecDate sets the "RecDate" field.
func (ec *EmissionCreate) SetRecDate(t time.Time) *EmissionCreate {
	ec.mutation.SetRecDate(t)
	return ec
}

// SetSize sets the "Size" field.
func (ec *EmissionCreate) SetSize(i int64) *EmissionCreate {
	ec.mutation.SetSize(i)
	return ec
}

// SetFreeFloat sets the "FreeFloat" field.
func (ec *EmissionCreate) SetFreeFloat(i int) *EmissionCreate {
	ec.mutation.SetFreeFloat(i)
	return ec
}

// SetNillableFreeFloat sets the "FreeFloat" field if the given value is not nil.
func (ec *EmissionCreate) SetNillableFreeFloat(i *int) *EmissionCreate {
	if i != nil {
		ec.SetFreeFloat(*i)
	}
	return ec
}

// SetLotSize sets the "LotSize" field.
func (ec *EmissionCreate) SetLotSize(i int) *EmissionCreate {
	ec.mutation.SetLotSize(i)
	return ec
}

// SetNillableLotSize sets the "LotSize" field if the given value is not nil.
func (ec *EmissionCreate) SetNillableLotSize(i *int) *EmissionCreate {
	if i != nil {
		ec.SetLotSize(*i)
	}
	return ec
}

// SetListingLevel sets the "ListingLevel" field.
func (ec *EmissionCreate) SetListingLevel(i int) *EmissionCreate {
	ec.mutation.SetListingLevel(i)
	return ec
}

// SetID sets the "id" field.
func (ec *EmissionCreate) SetID(x xid.ID) *EmissionCreate {
	ec.mutation.SetID(x)
	return ec
}

// SetNillableID sets the "id" field if the given value is not nil.
func (ec *EmissionCreate) SetNillableID(x *xid.ID) *EmissionCreate {
	if x != nil {
		ec.SetID(*x)
	}
	return ec
}

// SetTickerID sets the "Ticker" edge to the Ticker entity by ID.
func (ec *EmissionCreate) SetTickerID(id string) *EmissionCreate {
	ec.mutation.SetTickerID(id)
	return ec
}

// SetTicker sets the "Ticker" edge to the Ticker entity.
func (ec *EmissionCreate) SetTicker(t *Ticker) *EmissionCreate {
	return ec.SetTickerID(t.ID)
}

// Mutation returns the EmissionMutation object of the builder.
func (ec *EmissionCreate) Mutation() *EmissionMutation {
	return ec.mutation
}

// Save creates the Emission in the database.
func (ec *EmissionCreate) Save(ctx context.Context) (*Emission, error) {
	var (
		err  error
		node *Emission
	)
	ec.defaults()
	if len(ec.hooks) == 0 {
		if err = ec.check(); err != nil {
			return nil, err
		}
		node, err = ec.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*EmissionMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ec.check(); err != nil {
				return nil, err
			}
			ec.mutation = mutation
			if node, err = ec.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(ec.hooks) - 1; i >= 0; i-- {
			if ec.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = ec.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ec.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ec *EmissionCreate) SaveX(ctx context.Context) *Emission {
	v, err := ec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ec *EmissionCreate) Exec(ctx context.Context) error {
	_, err := ec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ec *EmissionCreate) ExecX(ctx context.Context) {
	if err := ec.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (ec *EmissionCreate) defaults() {
	if _, ok := ec.mutation.ID(); !ok {
		v := emission.DefaultID()
		ec.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ec *EmissionCreate) check() error {
	if _, ok := ec.mutation.RecDate(); !ok {
		return &ValidationError{Name: "RecDate", err: errors.New(`ent: missing required field "Emission.RecDate"`)}
	}
	if _, ok := ec.mutation.Size(); !ok {
		return &ValidationError{Name: "Size", err: errors.New(`ent: missing required field "Emission.Size"`)}
	}
	if _, ok := ec.mutation.ListingLevel(); !ok {
		return &ValidationError{Name: "ListingLevel", err: errors.New(`ent: missing required field "Emission.ListingLevel"`)}
	}
	if v, ok := ec.mutation.ID(); ok {
		if err := emission.IDValidator(v.String()); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "Emission.id": %w`, err)}
		}
	}
	if _, ok := ec.mutation.TickerID(); !ok {
		return &ValidationError{Name: "Ticker", err: errors.New(`ent: missing required edge "Emission.Ticker"`)}
	}
	return nil
}

func (ec *EmissionCreate) sqlSave(ctx context.Context) (*Emission, error) {
	_node, _spec := ec.createSpec()
	if err := sqlgraph.CreateNode(ctx, ec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*xid.ID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	return _node, nil
}

func (ec *EmissionCreate) createSpec() (*Emission, *sqlgraph.CreateSpec) {
	var (
		_node = &Emission{config: ec.config}
		_spec = &sqlgraph.CreateSpec{
			Table: emission.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: emission.FieldID,
			},
		}
	)
	if id, ok := ec.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := ec.mutation.RecDate(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: emission.FieldRecDate,
		})
		_node.RecDate = value
	}
	if value, ok := ec.mutation.Size(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt64,
			Value:  value,
			Column: emission.FieldSize,
		})
		_node.Size = value
	}
	if value, ok := ec.mutation.FreeFloat(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: emission.FieldFreeFloat,
		})
		_node.FreeFloat = value
	}
	if value, ok := ec.mutation.LotSize(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: emission.FieldLotSize,
		})
		_node.LotSize = value
	}
	if value, ok := ec.mutation.ListingLevel(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: emission.FieldListingLevel,
		})
		_node.ListingLevel = value
	}
	if nodes := ec.mutation.TickerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   emission.TickerTable,
			Columns: []string{emission.TickerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeString,
					Column: ticker.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ticker_emissions = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// EmissionCreateBulk is the builder for creating many Emission entities in bulk.
type EmissionCreateBulk struct {
	config
	builders []*EmissionCreate
}

// Save creates the Emission entities in the database.
func (ecb *EmissionCreateBulk) Save(ctx context.Context) ([]*Emission, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ecb.builders))
	nodes := make([]*Emission, len(ecb.builders))
	mutators := make([]Mutator, len(ecb.builders))
	for i := range ecb.builders {
		func(i int, root context.Context) {
			builder := ecb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EmissionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ecb *EmissionCreateBulk) SaveX(ctx context.Context) []*Emission {
	v, err := ecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ecb *EmissionCreateBulk) Exec(ctx context.Context) error {
	_, err := ecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ecb *EmissionCreateBulk) ExecX(ctx context.Context) {
	if err := ecb.Exec(ctx); err != nil {
		panic(err)
	}
}
